const defaultState = {
    trades: []
};


const rolesReducer = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state
    }
};

export default rolesReducer;